from django.contrib import admin
#from .models import food_entry, exercise_entry

"""@admin.register(food_entry)
class food_entry_admin(admin.ModelAdmin):
    list_display = ('id','user','food_name','date','protein','carb','fat','calories_food')
    list_filter = ('date',)


@admin.register(exercise_entry)
class exercise_entry_admin(admin.ModelAdmin):
    list_display = ('id','user','exercise_name','date','calories_per_hour','exercising_time','calories_exercise')
    list_filter = ('date',)"""

